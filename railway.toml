# Railway deployment for Dengue ML Predictor
# https://docs.railway.app/deploy/config-as-code

[build]
builder = "nixpacks"

[deploy]
startCommand = "java -jar target/dengue-predictor-web.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
